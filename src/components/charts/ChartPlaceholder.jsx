/**
 * Hoody Economy: gamified cyclical-capital economy simulation
 * Copyright (c) 2026 Cihan Toraman
 */

const ChartPlaceholder = ({ message, height = 'h-64' }) => (
  <div className={`${height} flex items-center justify-center text-sm text-muted`}>{message}</div>
);

export default ChartPlaceholder;
